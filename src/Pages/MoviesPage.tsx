import { FC } from "react";
import {MovieListComponent} from "../Components/features";

export const MoviesPage: FC = () => {
    return (
        <div>
            <MovieListComponent/>
        </div>
    );
};